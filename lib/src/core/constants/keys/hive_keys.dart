abstract class HiveKeys {
  static const token = 'token';
  static const refreshToken = 'refresh';
}

abstract class HiveBoxNames {
  // * OFFLINE
  static const offlineKeys = 'offlineKeys';

  // * USERS
  static const users = 'users';
}
